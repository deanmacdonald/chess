const path = require('path');

module.exports = {
  entry: './src/index.js', // Entry point of your app
  output: {
    path: path.resolve(__dirname, 'build'),
    filename: 'bundle.js',
    publicPath: '/',
  },
  mode: 'development', // Change to 'production' for final build
  module: {
    rules: [
      {
        test: /\.(js|jsx)$/, // Handles JS and JSX
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader',
        },
      },
      {
        test: /\.css$/, // Handles CSS imports
        use: ['style-loader', 'css-loader'],
      },
      {
        test: /\.js$/,
        enforce: 'pre',
        use: ['source-map-loader'],
        exclude: [/node_modules\/chess\.js/],
      },
    ],
  },
  resolve: {
    extensions: ['.js', '.jsx'], // So you can omit extensions in imports
  },
  devServer: {
    static: path.join(__dirname, 'public'),
    historyApiFallback: true,
    port: 3000,
    open: true,
  },
  devtool: 'source-map', // Enables source maps for debugging
};
