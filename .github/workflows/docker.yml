name: Build and Push Chess App Container

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout Repo
        uses: actions/checkout@v3

      - name: 🐳 Set Up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: 📦 Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🔨 Build Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/chess-app:latest .

      - name: 🚀 Push Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/chess-app:latest

          build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Image
        run: docker build -t chess-app:latest .
